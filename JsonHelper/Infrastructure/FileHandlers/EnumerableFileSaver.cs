using System;
using System.Collections.Generic;
using System.Linq;

namespace JsonHelper.Infrastructure
{
    //internal class EnumerableFileSaver<T> : FileSaverBase<IEnumerable<T>>
    //{
    //    //public EnumerableFileSaver(FileNameBuilderBase<IEnumerable<T>> nameBuilder, 
    //    //    string directoryPath, string fileExtension) : base(nameBuilder, directoryPath, fileExtension)
    //    //{
    //    //}

    //    protected override IEnumerable<T> JoinExistingAndNewValues(IEnumerable<T> existingValues,
    //        IEnumerable<T> valuesToSave)
    //        => (existingValues ?? Enumerable.Empty<T>())
    //        .Concat(valuesToSave ?? Enumerable.Empty<T>());
    //}
}
